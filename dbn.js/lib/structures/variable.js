define(function (require, exports, module) {
  "use strict";

  /**
   * DBNVariable = encapsulates a variable name
   *
   * @constructor
   * @param{String} name The variable name
   */
  var DBNVariable = module.exports = function (name) {
      this.name = name;
  };

});
